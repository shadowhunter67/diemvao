export interface TdtuProgram {
  id: string;
  code: string;
  name: string;
  /** Nhóm chương trình theo cấu trúc Phụ lục 2 (chuẩn / tiên tiến / tiếng Anh / liên kết quốc tế...). */
  group: string;
}
