/**
 * Domain config, not a clock-derived value: UniScoreVN's "current admission year" only changes
 * when maintainers intentionally move the dataset/rules to a new admission cycle.
 */
export const CURRENT_ADMISSION_YEAR = 2026;
