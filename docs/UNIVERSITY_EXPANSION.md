# University Expansion

The public app keeps a runtime catalog so search and comparison remain useful even when a school has no calculator yet.

The detailed expansion backlog, source prioritization, canonical directory reconciliation, and reviewer notes are private operational data. Public catalog-only entries do not imply formula support.

Current coverage is derived by `npm run stats:coverage`.
