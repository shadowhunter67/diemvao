# Changelog — UniscoreVN

Lịch sử phát triển đầy đủ (Phase 1–16, Batch 6–7). Tách khỏi `CLAUDE.md` để giữ file đó gọn — chỉ đọc file này khi cần tra cứu quyết định cũ hoặc bối cảnh một thay đổi cụ thể. Entry mới nhất ghi ở cuối, giữ nguyên văn phong ghi chú tại thời điểm thực hiện (không viết lại sai lịch sử).

- Phase 2 (nhập điểm gốc theo phương thức Xét tuyển Tổng hợp HCMUT 2026, có ĐGNL): công thức + tham số nằm ở `src/config/admission-2026.ts`, không hard-code trong component hay `calculator.ts`.
- localStorage lưu input gần nhất, key `hcmut-score-input-v2` (đã version hóa từ MVP `hcmut-score-calculator:input:v1`, không migrate dữ liệu cũ).
- Điểm ưu tiên KV/ĐT hiện nhập trực tiếp theo thang 30 (chưa có dropdown Khu vực/Đối tượng vì thiếu bảng mapping đối tượng ưu tiên chính thức).
- Phase 3 (target calculator + scenario simulator + share URL): `src/lib/targetCalculator.ts` chứa `calculateRequiredDgnl` (binary search, tái sử dụng `calculateAdmissionScore`). `src/lib/urlState.ts` serialize/parse query params (chỉ field hợp lệ). Target lưu localStorage riêng, key `hcmut-score-target-v1`. URL query params có precedence cao hơn localStorage khi load trang.
- Phase 4 (ngành mục tiêu + điểm chuẩn tham khảo): `src/data/hcmut-programs.ts` + `hcmut-cutoffs.ts` (29 chương trình, nguồn báo chí dẫn công bố chính thức HCMUT — trang hcmut.edu.vn nhúng bảng điểm dạng ảnh nên không fetch trực tiếp được). `calculateAdmissionScoreFromWeightedDgnlRaw` (đổi tên từ `calculateScoreForWeightedRaw` ở Phase 3, refactor bỏ hack nhét weightedRaw vào field ĐGNL giả — giờ tái sử dụng thẳng `convertThptScore`/`convertTranscriptScore`/`calculateAcademicScore`/`calculateBonus`/`calculatePriority`). Ngành/buffer/compare lưu localStorage `hcmut-score-program-v1`, mở rộng `urlState.ts` với `program`/`buffer`/`compare`. Tuyệt đối không gọi kết quả so sánh là "xác suất đậu"/"đậu"/"rớt" — chỉ "tham khảo"/"chênh lệch".
- Phase 5 (redesign "Admission Dashboard"): bỏ hẳn layout "form dài trái + result sticky phải" (`.admission-layout*` trong `index.css`, `ScoreForm.tsx`, `ScoreResult.tsx`, `ScoreBreakdown.tsx` — đã xóa). IA mới: `DashboardHero` (`CurrentScoreCard` + `SelectedProgramCard`) → 4 section input có `SectionHeader` đánh số 01-04 → `ScenarioSimulator` → `TargetSection` (full-width, có nút "Dùng trong mô phỏng" seed lại simulator qua `simulatorSeed`/`simulatorKey` state, cùng cơ chế remount với `resetToken`) → `ProgramSection` (đổi từ `<select>` sang custom search+list cho 29+ ngành, id `program-picker`) → `ProgramHistoryCompare` (id `history-section`) → `FormulaExplanation` (giờ là `<details>` đóng mặc định, id `formula-section`) → `Footer`. Design token khai báo trong `@theme` ở `index.css` (Tailwind v4 tự sinh cả CSS variable lẫn utility class: `bg-bg`, `text-ink`, `text-muted`, `bg-primary`, `text-accent`, `bg-success/warning/danger`, `rounded-card`, `shadow-card`). Không đổi bất kỳ hàm nào trong `lib/`, `config/`, `data/`, `types/` — thuần redesign UI.
- `BonusPrioritySection.tsx` có thêm 2 dropdown "Khu vực" + "Đối tượng ưu tiên" (local state trong component, không đụng App/config/calculator) tự cộng điểm theo bảng quy định chung toàn quốc (Bộ GD&ĐT, thang 30: KV1=0.75/KV2-NT=0.5/KV2=0.25/KV3=0, Nhóm ƯT1=+2.0/Nhóm ƯT2=+1.0) rồi điền vào đúng ô `priorityRaw30Scale` sẵn có — chỉ là input phụ trợ, ô nhập tay vẫn còn để override. Giá trị tối đa 2.75 khớp đúng `config.priority.maxRaw30Scale` nên user đã xác nhận dùng bảng này (2026-08-10), không phải bảng riêng của HCMUT.
- Chế độ nhập ĐGNL "Chi tiết" / "Tổng điểm" (`DgnlSection.tsx`, toggle `DgnlInputMode`): chế độ "Tổng điểm" cho nhập thẳng số trên phiếu ĐGNL thật (thang 1500, sau hệ số) thay vì 4 điểm thành phần. App.tsx branch tính `liveResult`/`requiredResult` theo mode — **cố tình không dựng DgnlInput giả** (4 số fake cộng đúng tổng) vì đúng comment gốc trong `targetCalculator.ts` gọi cách đó là "đánh lừa API"; thay vào đó tái dùng nguyên `calculateAdmissionScoreFromWeightedDgnlRaw`/`convertThptScore`/`convertTranscriptScore` (đã có sẵn, không sửa) để lắp lại `AdmissionResult`. Thêm 1 hàm MỚI (không sửa hàm cũ) `calculateRequiredDgnlFromWeightedRaw` trong `targetCalculator.ts` — bản sao logic binary-search của `calculateRequiredDgnl` nhưng lấy currentFinalScore từ weightedRaw thay vì từ AdmissionInput đầy đủ, để "Mục tiêu của bạn" vẫn đúng ở cả 2 chế độ. Đã hỏi và được user xác nhận trước khi đụng lib file (2026-08-10). State `dgnlModeState` lưu riêng, key `hcmut-score-dgnl-mode-v1`, KHÔNG serialize vào share URL (chưa làm, giới hạn đã biết).
- `ProgramSection.tsx`: đổi từ "chọn 1 ngành mục tiêu rồi mới bấm nút riêng để thêm so sánh" sang mỗi dòng trong danh sách có nút toggle "So sánh" độc lập — cho phép chọn nhiều ngành so sánh (tối đa 3, cap có sẵn trong `addProgramToComparison`) mà không cần đặt ngành đó làm mục tiêu. Prop đổi từ `isPinnedForComparison`/`canAddMoreComparison` sang truyền thẳng `comparisonProgramIds: string[]`. Không ảnh hưởng bảng "Lịch sử điểm chuẩn" (vẫn theo `selectedProgramId` riêng) hay bảng so sánh trong `ProgramHistoryCompare` (đã đọc từ `comparisonProgramIds` từ trước, không đổi).
- Phase 6 (fix "bố cục vẫn rối" sau feedback 2026-08-10, có tham khảo ý kiến độc lập từ `mcp__openai-bridge__ask_codex` đọc code trực tiếp): (1) 2 grid 2 cột bị stretch chiều cao không đều (Hero, Học bạ+THPT) gây khoảng trắng thừa — fix `items-start`, đã tổng quét toàn bộ `grid grid-cols-1` còn lại (đều là item đồng dạng, không cần fix). (2) Đảo IA: `TargetSection` + `ProgramSection` (chọn ngành/mục tiêu) chuyển từ sau Simulator lên ngay sau Hero, trước 4 section nhập điểm — theo đúng góp ý Codex về việc "thiết lập mục tiêu trước khi nhập điểm". (3) Phân cấp thị giác 2 tầng: card "chính" (Hero, 4 section 01-04) giữ nguyên `rounded-card bg-surface shadow-card`; card "công cụ" (Target/Program/Simulator/History/Formula) đổi sang `rounded-2xl bg-surface-soft` (bỏ shadow) — các box con bên trong đổi ngược từ `bg-surface-soft`→`bg-surface` để không bị chìm vào nền card mới.
- Phase 7 (sticky sidebar, 2026-08-10 — đảo ngược quyết định "non-sticky" ở Phase 5, user chủ động yêu cầu lại): desktop (`lg:` ≥1024px) `App.tsx` main chuyển `lg:grid lg:grid-cols-[1fr_340px]`, sidebar phải `lg:sticky lg:top-5`, KHÔNG dùng `items-start` (cố ý, để cell cao bằng cột nội dung chính → sidebar dính suốt hết trang thay vì nhả sớm). Container `max-w-5xl`→`max-w-7xl`. Chỉ render `heroElement` MỘT lần (biến JSX) rồi dùng `lg:order-1`/`lg:order-2` để đổi vị trí — tránh duplicate 2 bản JSX ẩn/hiện qua CSS (bài học ghi trong `index.css` gốc trước redesign). Mobile/tablet (`<lg`): `StickySummaryBar.tsx` — thanh gọn 1 dòng (điểm + ngành + chênh lệch) dính `top-0`, bấm vào cuộn mượt về đầu trang; KHÔNG ghim nguyên Hero chi tiết trên mobile vì chiếm gần hết viewport — đã giải thích lý do cho user. `TargetSection` + `ProgramBufferCard.tsx` (tách riêng từ "Biên mục tiêu" vốn nằm trong `ProgramSection`) đều chuyển vào sidebar, xếp dưới Hero — `ProgramSection` (chỉ còn search+list+so sánh) ở lại cột nội dung chính.
- Phase 7b (fix layout vỡ do sidebar làm hẹp cột chính, 2026-08-10): nhiều breakpoint nội bộ (Học bạ+THPT pairing, BonusPrioritySection outer split, DgnlSection 4-cột) đổi từ `sm:`/`lg:` sang `xl:` để không kích hoạt 2-3 cột khi cột chính chỉ còn ~600-700px (dropdown Khu vực/Đối tượng từng bị cắt chữ ở mức lg). `TargetSection` bỏ hẳn layout ngang `lg:grid-cols-[320px_1fr]` (chỉ dùng khi ở sidebar hẹp, gây vỡ) và box Hiện tại/Mục tiêu/Còn thiếu đổi từ `sm:grid-cols-3` sang xếp dọc — an toàn ở mọi bề rộng vì breakpoint Tailwind tính theo viewport chứ không theo container. Giới hạn so sánh ngành 3→5 (`MAX_COMPARISON_PINS`, export từ `lib/programs.ts`, dùng chung ở `ProgramSection`+`ProgramHistoryCompare` thay vì hằng số cục bộ trùng lặp).
- Phase 7c (`FormulaExplanation.tsx` vẽ lại thành sơ đồ luồng, 2026-08-10): thay 6 khối text đơn thuần bằng flow trực quan (3 node ĐGNL/THPT/Học bạ theo % trọng số → mũi tên → node "Điểm học lực" tô accent → mũi tên → 2 node Điểm cộng + Ưu tiên nối bằng dấu "+" → mũi tên → node "Điểm xét tuyển" cuối cùng tô đậm `bg-primary` chữ trắng). Icon `ArrowDown`/`Plus` từ lucide-react. Không đổi số liệu/công thức, chỉ trình bày lại — công thức Học bạ đã sửa cho khớp đúng cách tính trung bình 3 năm thật (không rút gọn sai như bản nháp đầu).
- Phase 8 (2026-08-10): (1) `ProgramSection.tsx` mục 02/03 (Học bạ + THPT) bỏ hẳn ghép ngang `xl:grid-cols-2` (App.tsx) — xuống dòng full-width như mục 04, vì kể cả `xl` (1280px) cũng không đủ chỗ khi trừ sidebar 340px (bảng Học bạ vẫn bị cắt/cuộn ngang). `FormulaExplanation.tsx` 3 node đầu (ĐGNL/THPT/Học bạ) đổi từ chuỗi công thức 1 dòng mono-font sang dạng chip từng thành phần (tên môn đầy đủ, không viết tắt TV/TA/TDKH) nối dấu "+" + khối phân số (gạch ngang) cho phần chuẩn hóa — dễ đọc hơn, không đổi số liệu. (2) `ThptSection.tsx` thêm `EnglishCertConverter` — quy đổi chứng chỉ tiếng Anh quốc tế (IELTS Academic/PTE Academic/TOEFL iBT/TOEFL iBT 2026/TOEIC) sang điểm môn Tiếng Anh thi THPT (thang 10), nguồn bảng: hcmut.edu.vn/tintuc/quy-doi-chung-chi-tieng-anh (user cung cấp screenshot nguồn 2026-08-10). TOEIC cần 2 mức Nghe-Đọc + Nói-Viết, lấy theo cặp thấp hơn nếu không đồng thời đạt (đúng ghi chú bảng gốc). Chỉ áp dụng điểm thi THPT (không áp cho học bạ — nguồn không đề cập). Vì `ThptSection` chỉ có field chung "Môn 2"/"Môn 3" (không gắn tên môn cụ thể theo tổ hợp), converter có 2 nút "Điền vào Môn 2"/"Điền vào Môn 3" để user tự chọn đúng ô theo tổ hợp của mình, không tự đoán.
- **Phase 9 — rebrand DiemVao + multi-school architecture (2026-08-10)**: đổi brand từ "HCMUT Score Calculator"/"HCMUT Admission Simulator" sang **DiemVao** ("Tính & mô phỏng điểm xét tuyển đại học"), HCMUT lùi thành school module đầu tiên. KHÔNG đổi bất kỳ công thức/số liệu nào (calculator engine, target binary search, scenario simulator giữ nguyên — 56 test cũ vẫn pass nguyên, không sửa expect nào).
  - **Di chuyển file** (`git mv`, giữ history): `src/config/admission-2026.ts` → `src/schools/hcmut/config/admission-2026.ts`; `src/lib/calculator.ts` + `.test.ts` → `src/schools/hcmut/calculator/calculator.ts`; `src/lib/targetCalculator.ts` + `.test.ts` → `src/schools/hcmut/calculator/targetCalculator.ts`; `src/lib/programs.ts` + `.test.ts` → `src/schools/hcmut/programs.ts`; `src/lib/urlState.ts` + `.test.ts` → `src/schools/hcmut/urlState.ts`; `src/lib/validation.ts` + `.test.ts` → `src/schools/hcmut/validation.ts`; `src/data/hcmut-programs.ts` → `src/schools/hcmut/data/programs.ts`; `src/data/hcmut-cutoffs.ts` → `src/schools/hcmut/data/cutoffs.ts`; `src/data/validateAdmissionDataset.ts` + `.test.ts` → `src/schools/hcmut/data/validateDataset.ts`; `src/types/{admission,form,programs}.ts` → `src/schools/hcmut/types/`. Toàn bộ import path trong `components/` + `App.tsx` sửa theo (sed hàng loạt, verify bằng `tsc -b --force` sạch 0 lỗi).
  - **Core mới** (thật sự generic, tách khỏi HCMUT): `src/core/round2.ts` (làm tròn 2 số — `schools/hcmut/calculator/calculator.ts` re-export lại để không đổi chữ ký import cũ); `src/core/rangeValidation.ts` (`FieldValidationResult` + `validateRange` — `schools/hcmut/validation.ts` re-export lại tương tự); `src/core/schoolModule.ts` — contract `SchoolModule { id, name, shortName, year }`, **cố tình KHÔNG** có `calculate()` chung chữ ký vì mỗi trường input schema khác nhau hoàn toàn (theo đúng yêu cầu "không ép universal formula engine").
  - **Registry**: `src/schools/hcmut/index.ts` export `hcmutModule: SchoolModule` (không phải barrel re-export toàn bộ submodule — component vẫn import trực tiếp theo path cụ thể, ví dụ `schools/hcmut/calculator/calculator`, tránh 1 file barrel khổng lồ khó theo dõi). `src/schools/index.ts` export `schoolRegistry` (`Record<string, SchoolModule>`) + `activeSchoolId = 'hcmut'` + `activeSchool` — KHÔNG dynamic plugin loading/DI, thêm trường mới = thêm 1 dòng registry. Test ở `src/schools/index.test.ts`.
  - **Site config**: `src/config/site.ts` export `siteConfig = { name: 'DiemVao', tagline, description }` — `Header.tsx`/`Footer.tsx`/`index.html` đọc từ đây, không hard-code brand text rải rác.
  - **UI**: `Header.tsx` hiện `siteConfig.name` + `siteConfig.tagline` làm brand chính, kèm badge `activeSchool.shortName` ("HCMUT") + `activeSchool.name` (tên đầy đủ trường) + "Xét tuyển tổng hợp {activeSchool.year}" làm school context — theo đúng mẫu "DiemVao / [HCMUT] Trường ĐH Bách khoa – ĐHQG TP.HCM / Xét tuyển tổng hợp 2026". `Footer.tsx` đổi tên hiển thị + disclaimer generic "không thuộc **các trường đại học** được hỗ trợ" (không chỉ riêng HCMUT). `index.html` title "DiemVao — Tính điểm HCMUT 2026" (brand đứng đầu, cụ thể hóa vì hiện chỉ có 1 trường) + description nhắc rõ multi-school.
  - **KHÔNG đổi**: localStorage keys (`hcmut-score-input-v2`/`hcmut-score-target-v1`/`hcmut-score-program-v1`/`hcmut-score-dgnl-mode-v1`) và URL query param keys (`dg_v`, `th_m`, `program`, `tg`...) — giữ nguyên để không phá dữ liệu/link cũ của người dùng, đúng yêu cầu "brand rename không phải lý do phá localStorage". `AdmissionCutoff`/tên hàm/tên biến vốn đã generic-sounding nhưng thuộc HCMUT (vd `AdmissionCutoff`, `validateAdmissionForm`) **không đổi tên** — rule "không rename vô tội vạ nếu không cần", chỉ đổi vị trí file.
  - `package.json` name: `hcmut-score-calculator` → `diemvao` (đã `npm install --package-lock-only` để đồng bộ `package-lock.json`, không đổi dependency/script nào).
  - Chưa làm (đúng scope Phase 9, để dành phase implement trường thứ 2): UIT/HCMUS/UEL calculator, router `/hcmut`/`/uit`, school switcher UI thật (chỉ có 1 trường nên chưa cần).
- Phase 10 (footer + link dự án, 2026-08-10): `siteConfig` (`src/config/site.ts`) thêm `githubUrl`/`issuesUrl` trỏ repo GitHub thật (`shadowhunter67/diemvao`) — không hard-code URL rải trong component. `Footer.tsx` viết lại: 2 cột (`sm:flex-row`) — trái là brand/tagline/disclaimer, phải là 3 link (GitHub, "Báo lỗi & góp ý" → GitHub Issues, "Nguồn dữ liệu" → anchor `#data-sources`) + dòng "© 2026 DiemVao" dưới cùng, border-top; không dùng card trắng để nhẹ hơn main content. Không dùng icon GitHub vì `lucide-react` 1.30 đã bỏ brand icon (kiểm tra `Object.keys(require('lucide-react'))` không có `Github`) — cố tình không thêm icon library mới chỉ vì 1 icon, dùng link text thuần (đã đủ accessible, không cần `aria-label`). `ProgramHistoryCompare.tsx` đổi `id="history-section"` → `id="data-sources"` (project chưa có section "Về dữ liệu" riêng — đây là section gần nhất chứa dữ liệu điểm chuẩn/nguồn, không tạo section/page mới). Header không thêm link GitHub (đã có Chia sẻ + Đặt lại, footer link được coi là đủ theo đúng yêu cầu "optional, skip nếu header đã chật"). README thêm 3 dòng Live/GitHub/Issues ở đầu file — **Live URL do user cung cấp, chưa tự verify đã deploy Vercel thật hay chưa** (ngoài phạm vi phase này).
- **Phase 11 — HCMUT applicant type + multi-school foundation (2026-08-10)**: research trước, code sau (đúng yêu cầu) — dùng 2 background research agent (7 trường ĐHQG-HCM khác + trường ngoài dùng ĐGNL) + tự research trực tiếp phần HCMUT non-DGNL, tổng hợp vào `docs/admission-research-2026.md`.
  - **HCMUT applicant type**: `schools/hcmut/types/applicantType.ts` — `HcmutApplicantType` 5 giá trị (dgnl/no-dgnl/international-certificate/foreign-high-school/special-program, khớp 8 "đối tượng 2.1-2.8" theo đề án). Chỉ `dgnl` (hành vi cũ, không đổi) và `no-dgnl` (mới) có công thức verify — 3 nhóm còn lại chỉ hiện thông báo "chưa hỗ trợ" qua `ApplicantTypeSection.tsx`, KHÔNG render calculator giả.
  - **Công thức no-dgnl**: `calculateAdmissionScoreNoDgnl` (`calculator.ts`) — điểm năng lực = THPT chuẩn hóa (tái dùng `convertThptScore`) × `config.noDgnl.abilityMultiplier` (0.75, nguồn: 2 cross-check độc lập, chưa fetch trực tiếp được PDF gốc hcmut.edu.vn — xem docs). Trả về đúng shape `AdmissionResult` (field `dgnl` chứa điểm năng lực đã quy đổi) để tái dùng nguyên `CurrentScoreCard`/`ScoreBreakdownDetails`/`StickySummaryBar`/`ProgramHistoryCompare` không cần nhánh UI riêng — chỉ đổi label "ĐGNL"→"Điểm năng lực" ở 2 chỗ tĩnh.
  - **Gating theo applicantType**: `App.tsx` — `hasCoreInput`/`liveResult`/`requiredResult` branch theo applicantType; `ScenarioSimulator` + phần "tính ngược ĐGNL cần đạt" trong `TargetSection` chỉ chạy khi `dgnl` (nhóm no-dgnl không có "ĐGNL tự do" để mô phỏng/binary-search). Nhóm chưa hỗ trợ: ẩn toàn bộ `<main>` calculator, chỉ hiện `ApplicantTypeSection` + thông báo.
  - **URL/localStorage**: query `at=` (`urlState.ts`: `parseApplicantTypeFromSearchParams`/`serializeApplicantTypeToSearchParams`), chỉ ghi vào URL khi khác mặc định `dgnl` — link chia sẻ cũ (không có `at`) vẫn hiểu đúng là dgnl, không breaking change. localStorage key mới `hcmut-applicant-type-v1`, độc lập với key cũ.
  - **School registry mở rộng**: `core/schoolModule.ts` thêm `SchoolStatus` ('supported'/'researching'/'formula-incomplete'). `schools/index.ts` thêm 7 trường ĐHQG-HCM đã research (UIT/UEL/HCMUS/USSH = researching, IU/AGU/UHS = formula-incomplete) — chỉ khai báo thông tin định danh, KHÔNG tạo thư mục `schools/<id>/` đầy đủ cho tới khi thật sự implement. `SchoolSelector.tsx` (modal, trigger "Đổi trường" ở `Header.tsx`) hiển thị đúng status, chỉ HCMUT bấm được.
  - **Nguồn dữ liệu**: `schools/hcmut/sources.ts` (`DataSource[]`) — hiển thị trong `ProgramHistoryCompare.tsx` (`#data-sources`).
  - **Quyết định KHÔNG implement trường thứ 2 trong phase này** dù UIT/UEL/HCMUS/USSH đã formulaVerified=true trên giấy — ưu tiên đảm bảo chất lượng/an toàn cho phần đã làm (research+applicant-type+registry) hơn tốc độ; xem lý do đầy đủ trong docs. Shortlist cho phase sau: UIT → UEL → HCMUS.
  - **Đã verify qua chrome-devtools trên dev server thật** trước khi commit: dgnl/no-dgnl/nhóm chưa hỗ trợ đều render đúng, không console error; SchoolSelector mở/đóng đúng.
  - Không làm (đúng scope): routing `/hcmut`/`/uit` (chỉ 1 trường thật, chưa cần), sửa double-scroll sidebar (đã cân nhắc nhưng rủi ro layout cao — xem lịch sử Phase 7/7b/7c — quyết định không đụng).
- **Phase 12 (2026-08-10, feedback ngay sau Phase 11)**: user muốn root ("/") hiện landing page thật thay vì vào thẳng calculator HCMUT — thay thế modal `SchoolSelector` (đã xóa) bằng `LandingPage.tsx` full-page (không dùng React Router, chỉ toggle state `view: 'landing' | 'calculator'` trong `App.tsx`, vẫn 1 route duy nhất nên không cần cấu hình SPA fallback Vercel). `loadInitialView()`: mặc định `'landing'`, NHƯNG nếu `window.location.search` khác rỗng (share link cũ dạng `?dg_v=...`, `?program=...`, `?at=...`) thì vào thẳng `'calculator'` luôn — đã verify qua chrome-devtools: share link cũ vẫn tính điểm đúng ngay, không bị chặn ở landing. `Header.tsx` "Đổi trường" giờ gọi `onChangeSchool` (set `view='landing'`) thay vì mở modal.
- **Phase 13 — rebrand DiemVao → Uniscore (2026-08-10)**: user chủ động muốn đổi tên để phản ánh rõ tầm nhìn multi-school hơn (khác Phase 9, lần này CÓ đổi cả localStorage key/URL — user xác nhận chấp nhận phá dữ liệu/link cũ, khác hẳn nguyên tắc "giữ nguyên key khi rebrand" ở Phase 9).
  - **Đổi tên hiển thị**: `siteConfig` (`config/site.ts`), `index.html` title/description, README.md, `docs/admission-research-2026.md`, và mọi chỗ hard-code "DiemVao" trong component (`LandingPage.tsx`, `ApplicantTypeSection.tsx`, `Footer.tsx`, `schools/hcmut/index.ts`, `core/schoolModule.ts`, `schools/hcmut/types/applicantType.ts`).
  - **package.json** name: `diemvao` → `uniscore`.
  - **localStorage keys** (`App.tsx`): `hcmut-score-input-v2`→`uniscore-input-v1`, `hcmut-score-target-v1`→`uniscore-target-v1`, `hcmut-score-program-v1`→`uniscore-program-v1`, `hcmut-score-dgnl-mode-v1`→`uniscore-dgnl-mode-v1`, `hcmut-applicant-type-v1`→`uniscore-applicant-type-v1` — **cố tình KHÔNG migrate dữ liệu cũ** (đúng tiền lệ MVP→v2 ở README dòng localStorage), người dùng cũ mất input đã lưu, phải nhập lại.
  - **URL query params** (`dg_v`, `th_m`, `program`, `tg`, `at`...) **giữ nguyên** — đã là mã ngắn trung tính, không mang tên brand nên không cần đổi dù user chọn "đổi đồng bộ".
  - **Repo/domain**: `githubUrl`/`issuesUrl` trong `site.ts` + README đổi sang `shadowhunter67/uniscore` — **đã verify thật** qua `gh repo view` (repo tồn tại, `main` branch OK) và `git remote set-url` + `git fetch` thành công. Domain production **chưa đổi**: verify qua `gh api repos/.../commits/<sha>/status` sau lần push Phase 14 cho thấy Vercel deploy target vẫn là `vercel.com/ntt9/diemvao/...` — project Vercel còn tên nội bộ "diemvao", live URL vẫn `diemvao.vercel.app` (README đã sửa lại đúng thực tế, không dùng placeholder `uniscore.vercel.app` nữa). Đổi tên project Vercel (Settings → General → Project Name) là việc user tự làm nếu muốn domain khớp brand mới.
- **Phase 14 — polish UX cùng lượt với Phase 13 (2026-08-10)**: sửa double-scroll sidebar tồn đọng từ Phase 7 (đã ghi "quyết định không đụng" ở Phase 11) — chuyển `TargetSection`+`ProgramBufferCard` ra khỏi cột sidebar `App.tsx` vào main-content (ngay sau `ProgramSection`), sidebar chỉ còn `heroElement`, bỏ `lg:overflow-y-auto lg:max-h-[...]`. `ProgramSection.tsx` thêm collapse-to-summary khi đã chọn ngành (nút "Đổi ngành" mở lại search+list). `CurrentScoreCard.tsx` thêm dòng nguồn nhỏ "Từ ĐGNL ĐHQG-HCM 2026"/"Từ quy đổi theo phương án HCMUT 2026" theo `applicantType`. `ProgramHistoryCompare.tsx` thêm badge "Đã đối chiếu nhiều nguồn" theo `source.kind !== 'official'` + dòng trust "Công thức đã xác minh theo nguồn được liệt kê" (badge này được thay bằng `verificationLabel` có kiểm soát hơn ở Phase 15). Không đổi business logic/công thức, 68 test cũ pass nguyên.
- **Phase 15 — kiến trúc multi-school kiểm chứng bằng UIT (2026-08-10)**: mục tiêu thêm trường thứ 2 thật để chứng minh (không chỉ lý thuyết) rằng App shell không cần sửa khi thêm trường.
  - **Research bổ sung UIT** (WebSearch agent + tự đọc trực tiếp qua chrome-devtools trang `tuyensinh.uit.edu.vn`, không phải suy đoán): xác minh được trọng số tổng (`THPT 47,5% + ĐGNL 47,5% + Học bạ 5%`, thang 100, điểm cộng ≤10) và ngưỡng đầu vào 2026 — đọc trực tiếp text từ trang chính thức. Xác minh được **điểm chuẩn trúng tuyển 19 ngành UIT 2026** — đọc trực quan (2 lần độc lập, phát hiện sửa 1 lỗi đọc mã ngành "Thiết kế vi mạch" lần đầu) từ ảnh công bố chính thức (bảng điểm nhúng dạng ảnh, không phải text). **Không tìm được** nguồn text cho cách chuẩn hóa chi tiết từng thành phần (hệ số môn THPT, quy đổi ĐGNL/1500, cách tính học bạ, công thức nhánh không-ĐGNL, bảng ưu tiên riêng) — quyết định (user chọn qua AskUserQuestion): **UIT phase này là trang thông tin + dữ liệu thật, KHÔNG dựng calculator** — tránh suy đoán công thức, đúng rule đề ra.
  - **App shell tách khỏi business logic**: toàn bộ thân `App()` cũ (state/effect/handler/JSX, ~414 dòng, 100% logic HCMUT) chuyển sang `schools/hcmut/HcmutCalculatorPage.tsx`, nhận props `{ onChangeSchool }`. `App.tsx` rút còn ~45 dòng: routing hook + tra `schoolRegistry[schoolId]` + render `school.Page` nếu có, ngược lại render `LandingPage`. `App.tsx` không còn import bất kỳ gì từ `schools/hcmut/calculator/...` hay `schools/uit/...` — chỉ import `schoolRegistry`, `LandingPage`, `Footer`.
  - **`core/schoolModule.ts`**: `SchoolModule` thêm field optional `Page?: (props: { onChangeSchool }) => ReactElement` — App shell chỉ biết render `Page`, không biết bên trong là calculator hay trang thông tin.
  - **Routing thật, không thêm router lib**: `src/hooks/useRoute.ts` (hand-rolled, `window.location.pathname` + `popstate` + `pushState`). Route `/` → landing, `/<id>` → tra registry. Backward-compat: `/` + query non-rỗng (share link cũ, luôn là HCMUT) → coi như `/hcmut`, `history.replaceState` canonicalize URL bar (không tạo flash landing→calculator vì `resolveSchoolId` xử lý đồng bộ ngay lần render đầu, effect chỉ dọn URL). Thêm `vercel.json` (`rewrites` → `index.html`) cho SPA fallback khi F5 ở `/hcmut`/`/uit` trên Vercel — dev server Vite đã tự fallback, không cần cho local.
  - **Storage namespace + migration**: `core/storage.ts` — `getSchoolStorageKey(schoolId, domain, version)` → `uniscore:<schoolId>:<domain>:v<version>`; `readWithMigration(newKey, legacyKeys, parse)` generic, thử newKey trước, không có thì thử từng legacyKey theo thứ tự, hợp lệ đầu tiên thì migrate sang newKey, không cái nào hợp lệ thì trả null (không crash). Áp dụng cho cả 5 key HCMUT, legacy chain 2 đời (`uniscore-*-v1` → `hcmut-score-*-v2/v1`), không chase ngược đời MVP (đã đổi shape, quyết định cũ giữ nguyên). Không xóa legacy key. Test: `core/storage.test.ts`.
  - **Trust metadata mở rộng**: `core/trust.ts` — `VerificationLevel` (`verified`/`official-source-available`/`cross-checked`/`incomplete`) + `verificationLabel()`. `DataSource` (HCMUT `sources.ts`) thêm field `verification` (additive, giữ `kind` cũ), gán lại theo evidence thật (không mặc định lạc quan). `ProgramHistoryCompare.tsx` đổi từ check blunt `kind !== 'official'` sang `verificationLabel(source.verification)`.
  - **Module UIT** (`schools/uit/`, độc lập hoàn toàn, không import từ `schools/hcmut/*`): `types/programs.ts` (`UitProgram`/`UitCutoff`, field naming khớp HCMUT có chủ đích nhưng chưa tạo type dùng chung ở `core/` — chưa có consumer thật cho cross-school search); `data/programs.ts` + `data/cutoffs.ts` (19 ngành + điểm chuẩn 2026 thật, mỗi cutoff có `note` ghi rõ "đọc trực quan từ ảnh"); `data/validateDataset.ts` + test (mirror pattern HCMUT); `sources.ts` (3 nguồn `verified`, không liệt kê phần chuẩn hóa thiếu nguồn như một "nguồn đã xác minh"); `UitInfoPage.tsx` (trọng số đã biết + khối "chưa đủ dữ liệu để tính điểm" liệt kê rõ thiếu gì + bảng điểm chuẩn thật + nguồn — KHÔNG có form tính điểm); `index.ts` export `uitModule` (`status: 'researching'`, có `Page`).
  - **`Header.tsx` sửa lỗi kiến trúc phát hiện giữa chừng**: trước đó hard-code `import { activeSchool } from '../schools'` (hằng số tĩnh 1-trường từ Phase 9) — đúng vấn đề mà phase này phải xóa bỏ. Đổi sang nhận `school: { shortName, name, year }` qua props (mỗi page tự truyền school của mình), xóa hẳn `activeSchool`/`activeSchoolId` khỏi `schools/index.ts` (không còn ai dùng). `onReset` đổi thành optional — trang không có form/localStorage (UitInfoPage) không hiện nút "Đặt lại" giả không làm gì.
  - **LandingPage.tsx**: status text tách khỏi button label (`STATUS_TEXT` mô tả trạng thái, button label riêng "Tính điểm"/"Xem thông tin"). Clickability đổi từ `status === 'supported'` sang `school.Page !== undefined` — UIT bấm được (dẫn `/uit`) dù status vẫn `researching`; 6 trường còn lại (chưa có `Page`) vẫn bất động, hiện "Chưa mở". `onSelectSchool` giờ thực dùng `schoolId` nhận được để `navigate`.
  - **QA**: 80 test pass (68 cũ + 6 storage + 4 UIT dataset + 2 registry mới), lint/build sạch, bundle 296.57KB (gzip 84.15KB, +3KB gzip so trước UIT). Chrome-devtools: `/`, `/hcmut`, `/uit` load trực tiếp + reload không lỗi (Vite dev SPA fallback); back/forward đúng qua `popstate`; migration test thật (seed `hcmut-score-input-v2` giả → vào `/hcmut` → tự migrate sang `uniscore:hcmut:input:v1`, autofill đúng, legacy key không bị xóa); share link cũ `/?dg_v=...` canonicalize đúng sang `/hcmut?dg_v=...`; breakpoint 390/768/1024/1440 không overflow, không console error ở cả `/hcmut` và `/uit`.
  - **Trả lời câu hỏi kiến trúc cốt lõi** ("thêm UEL ngày mai có cần sửa App.tsx/HCMUT module không?"): **Không.** Bằng chứng từ việc thêm UIT: chỉ cần (1) tạo `schools/<id>/` với ít nhất `index.ts` export `SchoolModule` (kèm `Page` nếu có UI), (2) thêm 1 dòng vào `schoolRegistry` ở `schools/index.ts`. `App.tsx` và toàn bộ `schools/hcmut/` không bị đụng một dòng nào trong suốt phase này (ngoại trừ tách nội dung ra `HcmutCalculatorPage.tsx`, một lần duy nhất, đã xong). Nợ kỹ thuật còn lại: `Header.tsx`/`Footer.tsx` vẫn là component dùng chung nhưng còn 1 vài chỗ giả định ngầm (vd `siteConfig` global) — chấp nhận được vì đó là brand-level, không phải school-level. `useRoute` chưa có unit test riêng (thin browser-coupled hook, coverage qua manual QA).
- **Phase 16 — UIT thành Admission Explorer thật, exact calculator vẫn blocked (2026-08-10)**: user cung cấp 7 URL nguồn chính thức UIT cụ thể, yêu cầu đọc sâu trước khi nâng cấp UIT từ "trang thông tin tối giản" lên "công cụ hữu ích thật" mà không bật calculator nếu chưa đủ nguồn.
  - **Research bổ sung** (WebFetch cho 3 URL đọc được text thuần + chrome-devtools cho 4 URL bị WebFetch timeout DNS, kể cả 1 PDF hóa ra là ảnh nhị phân không đọc được): xác nhận cấu trúc công thức đầy đủ hơn (`THPT = Max(THPT_ĐT, THPT_QĐ, THPT_QT)`, `ĐGNL = Max(ĐGNL_ĐT, ĐGNL_QĐ, ĐGNL_QT)`, quy đổi theo "phương pháp bách phân vị" — có nêu tên nhưng không có bảng cụ thể, vẫn blocked). **Phát hiện quan trọng nhất**: bảng điểm cộng đầy đủ, đọc được nguyên văn text (không phải ảnh) — 4 nhóm mức trần cố định 10/5/5/5 (không thang trượt theo hạng), cap tổng 10/100, đủ chuẩn "exact table" để tính chính xác. Xác nhận 2 tầng ngưỡng chứng chỉ quốc tế khác mục đích (đăng ký minh chứng thấp hơn vs chất lượng đầu vào cao hơn). Xác nhận tuyển thẳng (Điều 8) là route tách biệt hoàn toàn. Đối chiếu lại 19 điểm chuẩn đã lưu — khớp 100%, không sửa. Chi tiết đầy đủ ghi ở `docs/admission-research-2026.md` mục UIT "Research bổ sung 2026-08-10".
  - **Bật được (Level C, exact)**: `schools/uit/data/bonus.ts` + `bonus.ts` (`calculateUitBonus`, pure fn, cap từng nhóm + cap tổng) + `getAcademicCompetitionSubjects(programId)` (bảng môn↔ngành loại trừ Văn/Hóa, thêm Sinh/Sử-Địa/Nhật theo đúng ngành, KHÔNG suy rộng sang biến thể tiếng Anh nếu nguồn không nêu rõ) — UI `components/BonusChecker.tsx`. `schools/uit/data/thresholds.ts` + `eligibility.ts` (`checkThptThreshold`/`checkDgnlThreshold` có nhánh Thiết kế vi mạch riêng cần thêm điểm Toán; `checkCertificateRegistration`/`checkCertificateQuality` tách biệt 2 tầng, không gộp) — UI `components/EligibilityChecker.tsx`, hiện rõ overall pass nếu thỏa MỘT trong 3 điều kiện (OR).
  - **Info-only (Level A, không tính toán)**: `schools/uit/data/directAdmission.ts` + `components/DirectAdmissionSection.tsx` — bảng điều kiện tuyển thẳng theo môn/ngành, heading nói rõ "không cộng vào điểm xét tuyển tổng hợp".
  - **Vẫn blocked (Level D)**: công thức bách phân vị cụ thể, cách tính học bạ, quy đổi SAT/ACT→ĐGNL, quy đổi IB/A-Level→THPT — UitInfoPage.tsx liệt kê đúng 4 gap này (bỏ "bảng ưu tiên riêng" khỏi danh sách cũ vì không có bằng chứng cụ thể, tránh liệt kê mơ hồ).
  - **Data model UIT mở rộng (additive)**: `UitCutoff` thêm `scoreScale`/`publishedAt`. `sources.ts` thêm 3 nguồn mới (bonus/ngưỡng đăng ký chứng chỉ/tuyển thẳng), đều `verified` (đọc trực tiếp text). **Cố tình KHÔNG** thêm field `status: 'planned'|'final'|'superseded'` (generic versioning) — rà lại toàn bộ nguồn, không có case nào thật sự bị supersede (2 tầng ngưỡng chứng chỉ là 2 mục đích khác nhau, không phải v1→v2 của cùng fact) — hoãn tới khi có case thật, đúng tinh thần nhất quán từ Phase 15.
  - **`SchoolModule.summary`** (field optional mới): UIT set summary mô tả capability thật ("Dữ liệu tuyển sinh 2026 đầy đủ... Calculator chính xác đang chờ bảng quy đổi bách phân vị") — `LandingPage.tsx` ưu tiên hiện `summary` nếu có, fallback `STATUS_TEXT` mặc định cho 6 trường chưa set (không đổi gì ở UEL/HCMUS/USSH/IU/AGU/UHS).
  - **QA**: 100 test pass (80 cũ + 10 bonus + 10 eligibility), lint/build sạch, bundle 312.49KB (gzip 88.12KB, +4KB gzip). Chrome-devtools: boundary chính xác tại ngưỡng (717 pass/716.9 fail), bonus cap đúng khi chọn cả 4 nhóm (25 điểm gộp → hiện đúng 10/10), chọn ngành Thiết kế vi mạch hiện đúng field Toán bổ sung, LandingPage hiện summary mới, HCMUT không regression, breakpoint 390/768/1024/1440 sạch.
  - Không đụng `App.tsx`/`schools/hcmut/` trong suốt phase này.
- **Batch 6 — method-level capability + ApplicantProfile runtime dùng chung HCMUT/UEH/UEL + V-ACT consistency centralized + chặn contamination chứng chỉ THPT vào factual profile.** UEL vẫn `status: 'partial'` (chưa đủ nguồn cho exact calculator).
- **Batch 7 — rebrand Uniscore → UniscoreVN + fix bug THPT ghi đè transcript thành 0 (2026-08-13)**:
  - **Rebrand**: brand hiển thị `UniscoreVN`, slug `uniscorevn`. `siteConfig` (`src/config/site.ts`) là single source of truth, thêm field `slug`/`canonicalUrl` (`https://uniscorevn.vercel.app`). `package.json`/`package-lock.json` name → `uniscorevn` (`npm install --package-lock-only`, không đổi dependency). `index.html` title/description + thêm `<link rel="canonical">`. Toàn bộ text hiển thị user (LandingPage, Footer, ApplicantTypeSection, UitInfoPage, UehExplorerPage, UelExplorerPage, BonusChecker, TranscriptSection) đổi "Uniscore"/"UniScore" → "UniscoreVN"; comment nội bộ giải thích brand hiện tại cũng đổi theo (KHÔNG sửa các đoạn nhắc lịch sử "DiemVao → Uniscore" ở Phase 13 — đó là sự kiện đã xảy ra, sửa sẽ sai lịch sử). GitHub repo/URL (`shadowhunter67/uniscore`) **giữ nguyên** — chưa đổi tên repo (external action, ngoài phạm vi code).
  - **Storage rebrand có migration** (khác Phase 13 — lần đó cố tình KHÔNG migrate khi đổi brand): `core/storage.ts` `getSchoolStorageKey` đổi prefix `uniscore:` → `uniscorevn:`; mỗi `*_LEGACY_KEYS` ở `HcmutCalculatorPage.tsx` thêm 1 đời legacy mới ở đầu chain (key namespaced cũ dưới brand "Uniscore", vd `uniscore:hcmut:input:v1`) đứng trước 2 đời cũ hơn (`uniscore-*-v1` Phase 13, `hcmut-score-*` Phase 9) — không xóa/sửa đời cũ. `core/applicantProfileStorage.ts`: `APPLICANT_PROFILE_STORAGE_KEY` → `uniscorevn:applicant-profile:v1`, thêm `APPLICANT_PROFILE_LEGACY_KEYS = ['uniscore:applicant-profile:v1']`, migrate qua `readWithMigration` có sẵn. Priority: key mới thắng nếu đã có; legacy chỉ đọc khi key mới chưa tồn tại; legacy hỏng → bỏ qua an toàn, không crash.
  - **Tombstone khi xóa hồ sơ dùng chung**: thêm `clearStoredApplicantProfile()` (`applicantProfileStorage.ts`) xóa CẢ current key lẫn legacy key, gọi từ `ApplicantProfileContext.tsx` `clearProfile` TRƯỚC `setProfile({})` — nếu chỉ xóa current key, `readWithMigration` sẽ đọc lại legacy key cũ ở lần load kế tiếp và "hồi sinh" hồ sơ đã xóa. Test: `applicantProfileStorage.test.ts` describe `storage migration uniscore:* → uniscorevn:* (Batch 7)`.
  - **Fix bug "THPT ghi đè transcript thành 0"**: nguyên nhân gốc — 2 bug riêng ở `schools/hcmut/applicantProfileMapper.ts`/`HcmutCalculatorPage.tsx`: (1) 1 cờ `hasUserEditedAcademicFields` dùng chung cho cả THPT lẫn học bạ → sửa THPT cũng trigger ghi `next.transcript` bằng dữ liệu form (kể cả rỗng); (2) `mapThptScores` nhận `ThptInput`/`TranscriptInput` đã "làm tròn rỗng về 0" (từ `validateRange`, field `value: 0` khi `isEmpty: true` — tolerant cho calculator realtime, đúng thiết kế) rồi ghi thẳng số đó vào `ApplicantProfile`, không phân biệt "chưa nhập" (`isEmpty: true`) với "nhập thật 0". Fix: tách `hasUserEditedThptFields`/`hasUserEditedTranscriptFields` độc lập (đổi tổ hợp môn set cả 2 vì ảnh hưởng key mapping của cả hai); mapper đổi input type sang `{ value, isEmpty }` per-field (truyền thẳng `errors.thpt`/`errors.transcript` từ `validateAdmissionForm`, KHÔNG phải `currentInput`), field `isEmpty` bị loại khỏi map thay vì ghi `0`. KHÔNG đổi `validateRange`/calculator — ranh giới fix nằm đúng ở mapper (form → ApplicantProfile), calculator vẫn tolerant `0` cho input rỗng như cũ (đã verified, không đổi số liệu). Test: `applicantProfileMapper.test.ts` describe "Batch 7 — missing ≠ 0 và THPT/transcript ghi độc lập".
  - **README audit** (theo yêu cầu review lần 2 sau khi báo cáo lần đầu): cập nhật "Trường đang hỗ trợ" phản ánh đúng UEH/UEL có bảng quy đổi ĐGNL verified + đọc từ `ApplicantProfile` dùng chung (không chỉ nói "chưa có calculator"); thêm đoạn giải thích method-level capability (`AdmissionMethodDescriptor`) là nguồn sự thật ở UEH/UEL/UIT; cây thư mục "Kiến trúc multi-school" thêm `core/applicantProfile*`/`core/admissionMethod.ts` thay vì chỉ liệt kê generic cũ; ghi rõ hồ sơ dùng chung KHÔNG bao giờ chứa điểm đã quy đổi/điểm cuối riêng trường nào.
  - **A11y fix UEH/UEL** (phát hiện qua console `mcp__chrome-devtools` lúc verify, scope nhỏ nên fix luôn thay vì để nợ): input/select thiếu `id`/`name`/label liên kết ở `UehExplorerPage.tsx` (ô nhập ĐGNL, ô nhập điểm xét tuyển, select cơ sở) và `UelExplorerPage.tsx` (ô nhập ĐGNL, ô nhập điểm thi THPT, 2 ô điểm ưu tiên nâng cao) — thêm `id`/`name`/`aria-label` (2 ô đã có `<label>` text sẵn thì nối `htmlFor`/`id` thay vì thêm `aria-label` trùng lặp). Không đổi layout/logic tính điểm.
  - **Share-link round-trip test mới** (`urlState.test.ts`, case chưa tự động verify ở lần review đầu Batch 7): serialize state đầy đủ (DGNL chi tiết + THPT + học bạ 3 năm + applicant type + subject context + program/buffer/compare) → parse lại → so khớp nguyên trạng; case default (không ghi field thừa); case legacy URL cũ (không có `at`/`sj2`/`sj3`) vẫn parse đúng, fallback default an toàn.
  - **Storage migration audit bổ sung**: thêm test `core/storage.test.ts` đi qua đúng chain 3 đời thật (`hcmut-score-input-v2` → `uniscore-input-v1` → `uniscore:hcmut:input:v1`) khi chỉ đời cũ nhất có data, xác nhận không tự "phát minh" đời nào không tồn tại thật.
  - **QA cuối**: 265 test cũ (baseline batch 6, tất cả pass nguyên không sửa expect nào) + 20 test mới (6 mapper missing≠0/độc lập trong `applicantProfileMapper.test.ts`, 5 storage migration trong `applicantProfileStorage.test.ts`, 1 storage chain 3-đời trong `storage.test.ts`, 5 `siteConfig` brand trong `site.test.ts`, 3 share-link round-trip trong `urlState.test.ts`) — tổng 285 test pass, lint 0 warning, build sạch. Đã chạy `mcp__chrome-devtools` verify UI thật trên dev server 2 lượt (lần đầu + lần review lại sau a11y fix): title/brand đúng ở `/`, `/hcmut`, `/ueh`, `/uel`, `/uit`; migration legacy `uniscore:*`→`uniscorevn:*` đúng, score không đổi; THPT-only/transcript-only edit qua localStorage inspect đúng; "Xóa hồ sơ dùng chung" không bị legacy resurrect; share-link cũ (`?dg_v=...&th_m=...&tg=...`) load trực tiếp hydrate đúng form + tính đúng điểm (test qua điều hướng thẳng URL, không đọc được clipboard trong môi trường automation nên không tự lấy link "Chia sẻ" vừa tạo — xác nhận qua unit test round-trip thay thế); console sạch ở `/ueh`/`/uel` sau fix a11y; không overflow ở 390px/1440px (lưu ý máy này 1440 bị OS clamp lên ~1920, xem ghi chú "Lưu ý kỹ thuật khi test" trong `CLAUDE.md`) trên `/`, `/hcmut`, `/ueh`, `/uel`.
